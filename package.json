{
  "scripts": {
    "start": "cd chatbot-frontend && npm start",
    "build": "cd chatbot-frontend && npm run build",
    "install-frontend": "cd chatbot-frontend && npm install",
    "install-backend": "cd chatbot-backend && npm install",
    "install-all": "npm run install-frontend && npm run install-backend",
    "dev": "cd chatbot-frontend && npm start"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "fluent-ffmpeg": "^2.1.3",
    "form-data": "^4.0.4",
    "node-fetch": "^3.3.2"
  }
}
